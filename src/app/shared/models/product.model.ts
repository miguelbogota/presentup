// import { firestore } from 'firebase/app';

/**
 * Product to be sell in the app.
 */
/*
export interface IProduct {
  id: string;
  displayName: string;
  previewUrl?: string;
  createdAt: firestore.FieldValue; // Use serverTimestamp for field
  price: number;
  description: string;
  isBundle: boolean;
  includesId?: string[]; // Array - Id of additional products
  benefits?: string[]; // Array
}
*/

/**
 * User history for the products purchased
 */
/*
export interface IUserProduct extends IProduct {
  includes?: IProduct[]; // Array - Duplicate data
  quantity?: number;
}
*/