// import { firestore } from 'firebase/app';
// import { IUserProduct } from './product.model';

/**
 * Representation of a payment to show as history.
 */
/*
export interface IPayment {
  id: string;
  status: 'approved' | 'denied' | 'pending';
  createdAt: firestore.FieldValue; // Use serverTimestamp for field
  items: IUserProduct[]; // Array - Duplicate data in case original product is edited the change does not happen in the user history
  totalPrice: number;
}
*/